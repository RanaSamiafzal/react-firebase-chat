# TODO: Fix Chat Window Scrolling Issue

- [x] Edit src/components/chatcomp/ChatWindow.jsx to add `overflow-hidden` to the outer div's className
- [x] Run the app to test the scrolling behavior (app is already running on port 3000)

